{
  "name": "vj-moving-app",
  "private": true,
  "version": "1.0.2",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync:version": "node scripts/sync-version.mjs",
    "release:patch": "npm version patch --no-git-tag-version && npm run sync:version && npm run build",
    "release:minor": "npm version minor --no-git-tag-version && npm run sync:version && npm run build",
    "release:major": "npm version major --no-git-tag-version && npm run sync:version && npm run build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "lucide-react": "^1.9.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "vite-plugin-pwa": "^1.2.0"
  }
}
